使用Windows11和PowerShell7。主软件为Helm3.17.3，辅助工具为Node.js24LTS与npm。Helm负责Chart语法检查、值合并、文件读取、模板循环、Release上下文和Kubernetes对象渲染，不能用手写清单或其它模板引擎替代。

作业只做离线发布设计，不连接Kubernetes集群、镜像仓库、广告平台或外部接口。运行账户需要读取input_data并在同级创建output的权限，不需要管理员权限。建议准备2核处理器、4GB内存和1GB可用磁盘。
